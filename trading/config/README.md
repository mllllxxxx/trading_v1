# Trading Config

Config files here will define profile selections and non-secret runtime
settings. Secrets remain in environment files outside the repo.

Trading policy must not live only in `.env` files.

